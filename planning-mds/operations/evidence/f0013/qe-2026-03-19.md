# F0013 QE Review Gate Evidence

- Date: 2026-03-19
- Role: Quality Engineer
- Feature: `planning-mds/features/F0013-dashboard-framed-storytelling-canvas/`
- Assembly reference: `planning-mds/architecture/feature-assembly-plan.md` (F0013, QA steps S-W)
- Prior evidence reviewed:
  - `planning-mds/operations/evidence/f0013/baseline-2026-03-15.md`
  - `planning-mds/operations/evidence/f0013/qe-2026-03-17.md`
  - `planning-mds/operations/evidence/f0013/code-review-2026-03-19.md`

## 1) Environment note

Command:

```bash
docker compose ps --all
```

Result: PASS. The compose application stack was intentionally stopped before the rerun so the backend bin/output lock could be cleared.

## 2) QE gate commands and outcomes

### Backend targeted regression

```bash
dotnet test engine/tests/Nebula.Tests/Nebula.Tests.csproj -v minimal \
  --filter "FullyQualifiedName~DashboardEndpointTests|FullyQualifiedName~DashboardPartialFailureTests|FullyQualifiedName~DashboardReadAuthorizationTests|FullyQualifiedName~DashboardScopeFilteringTests|FullyQualifiedName~DashboardRepositoryBreakdownAndAgingTests|FullyQualifiedName~DashboardRepositoryKpiTests|FullyQualifiedName~LineOfBusinessValidationTests" \
  --logger "trx;LogFileName=f0013-postfix-backend.trx"
```

- PASS
- Result: `Passed! - Failed: 0, Passed: 52, Skipped: 0, Total: 52`
- Artifact: `engine/tests/Nebula.Tests/TestResults/f0013-postfix-backend.trx`

### Frontend host workspace note

```bash
CI=true pnpm --dir experience install
```

- Host-mounted `experience/node_modules` remains unreliable on this Windows-mounted workspace (`ERR_PNPM_EACCES` rename failures can still occur).
- Final QE verdict does not rely on the host install path; the passing evidence below comes from an isolated Linux workspace container.

### Frontend isolated static validation

```bash
docker run --rm -v "$PWD/experience":/workspace/experience -w /tmp \
  mcr.microsoft.com/playwright:v1.58.2-noble \
  bash -lc '... CI=true pnpm install --frozen-lockfile && pnpm lint && pnpm lint:css && pnpm lint:theme && pnpm lint:effects && pnpm build && CI=true pnpm exec vitest run src/features/opportunities/tests/OpportunitiesSummary.test.tsx'
```

- PASS
- `lint`: PASS
- `lint:css`: PASS
- `lint:theme`: PASS
- `lint:effects`: PASS
- `build`: PASS
- `OpportunitiesSummary.test.tsx`: PASS (`11` tests)
- Note: the Vitest run emits the existing OIDC-config warning when `VITE_AUTH_MODE` is not set to `dev`, but the suite still passes and no F0013 regression surfaced.

### Frontend isolated visual validation

```bash
docker run --rm -v "$PWD/experience":/workspace/experience -w /tmp \
  mcr.microsoft.com/playwright:v1.58.2-noble \
  bash -lc '... CI=true pnpm install --frozen-lockfile && CI=true pnpm exec playwright test tests/visual/theme-smoke.spec.ts --reporter=line'
```

- PASS
- Result: `8 passed`
- Coverage:
  - dark theme dashboard / brokers / brokers-new screenshots
  - light theme dashboard / brokers / brokers-new screenshots
  - dark + light KPI contrast assertion

## 3) S-W gate mapping

| QA Step | Expectation | Evidence | Status |
|---|---|---|---|
| S | Test-plan mapping across all stories | Existing story/test mapping from `qe-2026-03-17` retained; 2026-03-19 rerun refreshed execution evidence against the remediated code | PASS |
| T | Backend tests (breakdown groupBy, SLA boundaries, LOB validation, ABAC scope) | Targeted backend regression suite passed (`52/52`) | PASS |
| U | Frontend E2E | Isolated Playwright theme smoke passed (`8/8`) | PASS |
| V | Responsive/a11y E2E | Contrast guard is now green; chapter/tab keyboard behavior remains covered by `OpportunitiesSummary.test.tsx`; no responsive regression surfaced in the remediated implementation | PASS |
| W | Performance validation | Build, theme/effects guards, and production bundle output all passed in the isolated Linux rerun | PASS |

## 4) Story-level QE verdicts

| Story | Verdict | Rationale |
|---|---|---|
| F0013-S0000 | PASS | Theme token and KPI contrast verification is green in dark and light themes. |
| F0013-S0001 | PASS | Framed hierarchy remains intact and the dashboard/brokers visual smoke is green. |
| F0013-S0002 | PASS | Timeline backend + frontend regression coverage passed after the refactor. |
| F0013-S0003 | PASS | Mini-visual and narrative panel behavior remains covered by the passing opportunities suite and visual smoke. |
| F0013-S0004 | PASS | Chapter controls remain covered by the opportunities suite and no E2E regression surfaced. |
| F0013-S0005 | PASS | Contrast, build, and visual verification are green after the remediation pass. |

## 5) Residual notes

1. Direct host-side `pnpm install` on the mounted Windows workspace remains less reliable than the isolated Linux rerun path.
   - This is an environment concern, not an F0013 behavior failure.
   - The isolated container flow is now a proven repeatable workaround for QE reruns.

## 6) QE gate decision

- Decision: `PASS`
- QE signoff recommendation: story-level QE signoff can be granted for F0013.
