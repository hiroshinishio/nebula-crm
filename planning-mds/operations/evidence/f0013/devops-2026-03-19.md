# F0013 DevOps Review Gate Evidence

- Date: 2026-03-19
- Role: DevOps
- Feature: `planning-mds/features/F0013-dashboard-framed-storytelling-canvas/`
- Scope: deployability, runtime/build health, infra artifacts, migrations, and seed-data readiness

## 1) Runtime / workspace state

Command:

```bash
docker compose ps --all
```

Result: PASS.

Observed state:
- The compose application stack is currently stopped.
- This was intentional for the rerun so the backend output lock could be cleared and clean build/test artifacts could be produced.

## 2) Infrastructure artifact validation

### Repo root

```bash
python3 agents/devops/scripts/validate-infrastructure.py . --strict
```

- PASS

### Subtree validator note

- Earlier same-day strict subtree checks on `experience` and `engine` still report missing per-subtree compose / Docker / CI files.
- These are not treated as F0013 deployment blockers because this repo’s deploy contract is rooted at the repository level (`docker-compose.yml`, `.env.example`, `experience/.env.example`, `experience/nginx.conf`) rather than as independent frontend/backend subtree deployments.

## 3) Build and deployability checks

### Backend build

```bash
dotnet build engine/src/Nebula.Api/Nebula.Api.csproj -v minimal
```

- PASS
- Result: `Build succeeded. 0 Warning(s), 0 Error(s).`

### Frontend isolated verification

```bash
docker run --rm -v "$PWD/experience":/workspace/experience -w /tmp \
  mcr.microsoft.com/playwright:v1.58.2-noble \
  bash -lc '... CI=true pnpm install --frozen-lockfile && pnpm lint && pnpm lint:css && pnpm lint:theme && pnpm lint:effects && pnpm build && CI=true pnpm exec vitest run src/features/opportunities/tests/OpportunitiesSummary.test.tsx'
```

- PASS
- Result:
  - `lint`: PASS
  - `lint:css`: PASS
  - `lint:theme`: PASS
  - `lint:effects`: PASS
  - `build`: PASS
  - targeted Vitest: PASS (`11/11`)

### Frontend isolated visual verification

```bash
docker run --rm -v "$PWD/experience":/workspace/experience -w /tmp \
  mcr.microsoft.com/playwright:v1.58.2-noble \
  bash -lc '... CI=true pnpm install --frozen-lockfile && CI=true pnpm exec playwright test tests/visual/theme-smoke.spec.ts --reporter=line'
```

- PASS
- Result: `8 passed`

## 4) F0013 deployment evidence review

What is present:
- F0013 migration exists: `engine/src/Nebula.Infrastructure/Persistence/Migrations/20260316014518_F0013_AddLineOfBusinessAndWorkflowSlaThresholds.cs`
- F0013 SLA seed/config exists: `engine/src/Nebula.Infrastructure/Persistence/Configurations/WorkflowSlaThresholdConfiguration.cs`
- Dev seed data populates `LineOfBusiness` values: `engine/src/Nebula.Infrastructure/Persistence/DevSeedData.cs`
- Runtime environment variables are documented in `.env.example` and `experience/.env.example`
- Frontend production routing/config exists in `experience/nginx.conf`
- Root deployment contract exists in `docker-compose.yml`
- Clean backend build proof now exists
- Clean frontend build/test proof now exists in an isolated Linux workspace

## 5) DevOps gate decision

- Assessment: `PASS`
- Risk level: `Low`
- Story-level DevOps signoff: `grantable`

Rationale:
- The feature’s migration/seed/runtime artifacts are present.
- The backend build is clean with the app stack stopped.
- The frontend build/test/visual verification is now proven in a repeatable isolated Linux workspace even though the host-mounted Windows workspace remains less reliable for direct `pnpm install`.

## 6) Follow-up recommendations (non-blocking)

1. If the team wants subtree validators green, calibrate those scripts to the repo-root deployment model or add the missing per-subtree docs/artifacts explicitly.
2. Keep using the isolated Linux workspace/container path for repeatable frontend gate reruns on the mounted Windows workspace.
