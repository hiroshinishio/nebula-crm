# F0013 Security Review Gate Evidence

- Date: 2026-03-19
- Role: Security Reviewer
- Feature: `planning-mds/features/F0013-dashboard-framed-storytelling-canvas/`
- Assembly reference: `planning-mds/architecture/feature-assembly-plan.md` (F0013 security scope: dashboard/breakdown authorization and data exposure boundaries)
- Prior evidence reviewed:
  - `planning-mds/operations/evidence/f0013/baseline-2026-03-15.md`
  - `planning-mds/operations/evidence/f0013/security-2026-03-16.md`
  - `planning-mds/operations/evidence/f0013/security-2026-03-17.md`
  - `planning-mds/operations/evidence/f0013/qe-2026-03-17.md`

## 1) Runtime health check (before security role)

Command:

```bash
docker compose ps --all
```

Result: PASS. `api`, `db`, `authentik-server`, `authentik-worker`, `temporal`, and `temporal-ui` were up; health checks were healthy where defined.

## 2) Security verification commands and outcomes

### Targeted ABAC/dashboard test run

```bash
dotnet test engine/tests/Nebula.Tests/Nebula.Tests.csproj -v minimal \
  --filter "FullyQualifiedName~DashboardReadAuthorizationTests|FullyQualifiedName~DashboardScopeFilteringTests|FullyQualifiedName~DashboardEndpointTests.GetOpportunityBreakdown|FullyQualifiedName~CasbinAuthorizationServiceTests.DashboardPolicy_MatchesPolicyCsv|FullyQualifiedName~CasbinAuthorizationServiceTests.ExternalUser_AllResources_Denied" \
  --logger "trx;LogFileName=f0013-security-2026-03-19-targeted.trx"
```

- FAIL: build/test execution blocked before tests completed
- Errors:
  - `MSB3021` copying `Npgsql.EntityFrameworkCore.PostgreSQL.dll` into `engine/src/Nebula.Api/bin/Debug/net10.0/` denied
  - `MSB3021` copying `Nebula.Domain.dll`, `Nebula.Application.dll`, and `Nebula.Infrastructure.dll` into `engine/src/Nebula.Api/bin/Debug/net10.0/` denied

### Security script bundle

```bash
python3 agents/security/scripts/security-audit.py planning-mds/security
python3 agents/security/scripts/security-audit.py planning-mds/security --strict
sh agents/security/scripts/check-secrets.sh
sh agents/security/scripts/scan-dependencies.sh
sh agents/security/scripts/run-sast-scan.sh
sh agents/security/scripts/run-dast-scan.sh
```

Results:
- `security-audit`: PASS (`rc=0`)
- `security-audit --strict`: PASS (`rc=0`)
- `check-secrets`: BLOCKED TOOLING (`rc=2`, `gitleaks` missing)
- `scan-dependencies`: BLOCKED ENV/NETWORK (`rc=124`, `pnpm audit` retried against npm registry and hit `EAI_AGAIN`; timeout expired)
- `run-sast-scan`: BLOCKED TOOLING (`rc=2`, `semgrep` missing)
- `run-dast-scan`: BLOCKED CONFIG (`rc=2`, target URL not configured)

## 3) Threat model focus (F0013-specific)

Assets:
- Dashboard aggregate and breakdown data (`/dashboard/opportunities/*`)
- LOB and SLA telemetry surfaced through narrative widgets

Actors:
- Internal scoped roles: `DistributionUser`, `DistributionManager`, `Underwriter`, `RelationshipManager`, `ProgramManager`, `Admin`
- `BrokerUser` (explicitly restricted on aggregate dashboard resources)

Trust boundaries:
- Browser -> API (`/dashboard/*`)
- Endpoint authorization (`HasAccessAsync` / Casbin role-policy checks)
- Service/repository data boundary (role scope filters)

## 4) OWASP Top 10 assessment (F0013 gate)

1. **A01 Broken Access Control**: PASS (endpoint role checks plus repository scope filtering remain in place; no new bypass surfaced in this run)
2. **A02 Cryptographic Failures**: PASS (no F0013 crypto changes)
3. **A03 Injection**: PASS (EF Core LINQ query paths)
4. **A04 Insecure Design**: PASS (scope contract remains implemented at query layer)
5. **A05 Security Misconfiguration**: PASS WITH RECOMMENDATIONS (DAST target and some scanner prerequisites are still missing from the local environment)
6. **A06 Vulnerable and Outdated Components**: INCONCLUSIVE (dependency scan blocked by registry/network access in current environment)
7. **A07 Identification and Authentication Failures**: PASS (dashboard group requires authentication)
8. **A08 Software and Data Integrity Failures**: PASS (no new integrity control regressions observed)
9. **A09 Security Logging and Monitoring Failures**: PASS WITH RECOMMENDATIONS (scanner/tooling coverage is still incomplete)
10. **A10 SSRF**: PASS (no server-side URL fetch introduced)

## 5) Findings by severity

### High/Critical

None in reviewed F0013 scope for this gate run.

### Medium

1. Security scanner gate is still incomplete in this environment.
   - Locations:
     - `agents/security/scripts/check-secrets.sh` (`gitleaks` not installed)
     - `agents/security/scripts/run-sast-scan.sh` (`semgrep` not installed)
     - `agents/security/scripts/run-dast-scan.sh` (target URL unset)
     - `agents/security/scripts/scan-dependencies.sh` (`pnpm audit` hit `EAI_AGAIN` and did not complete before timeout)
   - Why it matters:
     - Residual risk remains for dependency, secrets, SAST, and DAST findings outside the manually reviewed paths.
   - Remediation:
     1. Install scanner toolchain (`gitleaks`, `semgrep`) in the CI/runtime image.
     2. Provide a deterministic `DAST_TARGET_URL` in the security gate pipeline.
     3. Run dependency scanning in a network-enabled CI job and attach artifacts.

2. Fresh backend authz regression verification is blocked by local build output locking.
   - Location:
     - `engine/src/Nebula.Api/bin/Debug/net10.0/` (copy target denied during `dotnet test`)
   - Why it matters:
     - The security-sensitive dashboard authz/breakdown path could not be revalidated against the current repo state in this run.
   - Remediation:
     1. Clear the `Nebula.Api/bin` lock or run tests in an isolated build workspace.
     2. Re-run the targeted dashboard authz tests once the output directory is writable.

## 6) Control verification notes (ABAC/data boundaries)

- Endpoint authorization remains deny-by-default via `HasAccessAsync` over role-policy evaluation:
  - `engine/src/Nebula.Api/Endpoints/DashboardEndpoints.cs:40`
  - `engine/src/Nebula.Api/Endpoints/DashboardEndpoints.cs:52`
  - `engine/src/Nebula.Api/Endpoints/DashboardEndpoints.cs:94`
  - `engine/src/Nebula.Api/Endpoints/DashboardEndpoints.cs:104`
  - `engine/src/Nebula.Api/Endpoints/DashboardEndpoints.cs:126`
  - `engine/src/Nebula.Api/Endpoints/DashboardEndpoints.cs:202`
- Repository-level role scoping is still applied before aggregation:
  - `engine/src/Nebula.Infrastructure/Repositories/DashboardRepository.cs:27`
  - `engine/src/Nebula.Infrastructure/Repositories/DashboardRepository.cs:28`
  - `engine/src/Nebula.Infrastructure/Repositories/DashboardRepository.cs:29`
  - `engine/src/Nebula.Infrastructure/Repositories/DashboardRepository.cs:1280`
- Cross-scope filtering tests still exist for breakdown leakage boundaries:
  - `engine/tests/Nebula.Tests/Integration/DashboardScopeFilteringTests.cs:258`
- BrokerUser aggregate deny behavior remains explicitly tested:
  - `engine/tests/Nebula.Tests/Integration/DashboardReadAuthorizationTests.cs:34`

## 7) Story-level security verdicts

| Story | Verdict | Rationale |
|---|---|---|
| F0013-S0000 | CONDITIONAL PASS | No active ABAC/data-exposure flaw found; scanner/toolchain coverage is still incomplete. |
| F0013-S0001 | CONDITIONAL PASS | Dashboard read paths remain scoped; residual risk is scanner/runtime incompleteness. |
| F0013-S0002 | CONDITIONAL PASS | Timeline/flow endpoints still inherit scoped query controls. |
| F0013-S0003 | CONDITIONAL PASS | Breakdown endpoints still enforce role + scoped data filtering with leakage tests. |
| F0013-S0004 | CONDITIONAL PASS | Chapter-mode data endpoints still use the same secured scoped repository paths. |
| F0013-S0005 | CONDITIONAL PASS | Security parity remains intact for implemented controls; complete scanner gate is still pending. |

## 8) Security gate decision

- Assessment: `CONDITIONAL PASS`
- Risk level: `Medium` (tooling/scan coverage gaps and local build-output locking, not an active exploit in the reviewed F0013 paths)
- Release recommendation for F0013: acceptable for feature security controls, with required follow-up to complete scanner pipeline coverage and re-run the targeted authz regression once the build output lock is cleared.
