# F0013 DevOps Review Gate Evidence

- Date: 2026-03-19
- Role: DevOps
- Feature: `planning-mds/features/F0013-dashboard-framed-storytelling-canvas/`
- Scope: deployability, runtime health, infra artifacts, migrations, and seed-data readiness

## 1) Runtime health check

Command:

```bash
docker compose ps --all
```

Result: PASS.

Observed services:
- `api` up
- `db` healthy
- `authentik-server` healthy
- `authentik-worker` healthy
- `temporal` up
- `temporal-ui` up

Interpretation:
- Core runtime stack is available in the local compose environment.
- This is only a health snapshot, not a deployability guarantee.

## 2) Infrastructure artifact validation

### Repo root

Command:

```bash
python3 agents/devops/scripts/validate-infrastructure.py . --strict
```

Result: PASS.

### Frontend subtree

Command:

```bash
python3 agents/devops/scripts/validate-infrastructure.py experience --strict
```

Result: FAIL.

Missing items reported:
- `docker-compose.yml / compose.yml`
- `Dockerfile`
- `CI configuration directory`

### Backend subtree

Command:

```bash
python3 agents/devops/scripts/validate-infrastructure.py engine --strict
```

Result: FAIL.

Missing items reported:
- `docker-compose.yml / compose.yml`
- `CI configuration directory`
- `.env.example or .env.sample`

Interpretation:
- The repo root has enough infra artifacts for the minimum validator.
- The frontend subtree does not have an independent container/deploy contract.

## 3) Build and deployability checks

### Frontend build

Command:

```bash
pnpm --dir experience build
```

Result: FAIL.

Failure:
- `tsc: not found`

Interpretation:
- The frontend build cannot currently be proven from this workspace because the local toolchain is incomplete.

### Backend build

Command:

```bash
dotnet build engine/src/Nebula.Api/Nebula.Api.csproj -v minimal
```

Result: FAIL.

Failure:
- `MSB3021` access denied copying assemblies into `engine/src/Nebula.Api/bin/Debug/net10.0/`

Interpretation:
- The backend compiles far enough to restore and build project outputs, but the workspace/bin lock prevents a clean build artifact from being produced.

## 4) F0013 deployment evidence review

What is present:
- F0013 migration exists: `engine/src/Nebula.Infrastructure/Persistence/Migrations/20260316014518_F0013_AddLineOfBusinessAndWorkflowSlaThresholds.cs`
- F0013 SLA seed/config exists: `engine/src/Nebula.Infrastructure/Persistence/Configurations/WorkflowSlaThresholdConfiguration.cs`
- Dev seed data populates `LineOfBusiness` values: `engine/src/Nebula.Infrastructure/Persistence/DevSeedData.cs`
- Runtime environment variables are documented in `.env.example` and `experience/.env.example`
- Frontend production routing/config exists in `experience/nginx.conf`
- Backend compose service exists in `docker-compose.yml`

What is missing or not yet proven:
- No checked-in `experience/Dockerfile`
- No checked-in `planning-mds/architecture/deployment-architecture.md`
- No clean frontend build proof from this workspace
- No clean backend build proof from this workspace

## 5) DevOps gate decision

- Assessment: `FAIL`
- Risk level: `Medium`
- Story-level DevOps signoff: `not grantable yet`

Rationale:
- The deployable backend pieces are present, but the feature is not fully proven deployable end-to-end.
- Frontend deployment artifacts are incomplete at subtree level, and the current build path is blocked by missing local tooling.
- Backend build is blocked by a workspace/bin access issue, so I cannot record a clean build artifact for closeout.

## 6) Remediation required before DevOps PASS

1. Add or restore the frontend deployment artifact path, including a checked-in `experience/Dockerfile` or an equivalent documented production container path.
2. Resolve the local build environment so `experience` has `tsc` available and the frontend build can complete.
3. Clear the backend `bin/Debug/net10.0` access issue and rerun `dotnet build` or the closest release-equivalent validation.
4. Add the missing deployment architecture document if it is still intended to be part of the closeout contract.
